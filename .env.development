# 로컬 개발 기본값. 개인 키가 필요한 항목은 .env.local(gitignore됨)에 넣는다.
VITE_API_BASE_URL=/api

# 1원 인증 시연 연출(#366). 로컬 개발에서는 기본으로 켜둔다 — CODEF 데모 서버는 실제
# 이체를 하지 않아 은행 앱 알림이 오지 않으므로, 이 값이 false면 입금자명을 확인할
# 방법이 없어 계좌 연동을 끝까지 테스트할 수 없다.
VITE_DEMO_MODE=true
