// 카테고리 차트 전용 토큰(variables.css)만 쓴다. 도넛 차트와 범례가 같은 순서로
// 색을 매겨야 하므로 두 곳(CategoryDonutChart.vue, HomeView.vue)이 이 배열 하나를 같이 쓴다.
export const CATEGORY_CHART_PALETTE = [
  'var(--color-chart-leaf)',
  'var(--color-chart-teal)',
  'var(--color-chart-amber)',
  'var(--color-chart-sage)',
  'var(--color-chart-blue)',
]
